# AGENTS.md

Project guidance for coding agents working in **auto-exchange**.

## Agent skills

### Issue tracker

GitHub Issues on `Yang-dongwen/auto-exchange` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` + `docs/adr/` (created lazily by domain skills). See `docs/agents/domain.md`.

Backend module design docs already live under `okx-bot/doc/` and remain the source of truth for video / aigen / imggen / article until a shared glossary is built.
